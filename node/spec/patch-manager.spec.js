var patchManager = require('../lib/patch-manager.js');

describe("Patch Manager", function() {

    var failTest = function(error) {
        expect(error).toBeUndefined();
    };

    it("sends with sendmail", function(done) {

    });

});
